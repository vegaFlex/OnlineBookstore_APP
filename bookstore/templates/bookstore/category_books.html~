{% extends "bookstore/base.html" %}

{% block title %}Книги по категория{% endblock %}

{% block content %}
<aside class="w-1/4 p-4 bg-gray-800 text-white">
    <h2 class="font-bold text-lg mb-4">Категории</h2>
    <ul>
        {% for category in categories %}
        <li class="mb-2">
            <a href="{% url 'category_books' category.id %}" class="text-blue-600 hover:underline">{{ category.name }}</a>
        </li>
        {% endfor %}
    </ul>
</aside>

<main class="w-3/4 p-4">
    <h2 class="text-2xl font-bold mb-8">{{ category.name }}</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {% for book in books %}
        <div class="bg-white shadow-lg rounded-lg overflow-hidden">
            <a href="{% url 'book_detail' book.id %}">
{#                <img src="{{ book.cover_image.url }}" alt="{{ book.title }}" class="w-full h-48 object-cover">#}
                <img src="{{ book.cover_image.url }}" alt="{{ book.title }}" class="w-full h-auto max-h-64 object-contain">
            </a>
            <div class="p-4">
                <h3 class="text-lg font-semibold mb-2">
                    <a href="{% url 'book_detail' book.id %}" class="hover:underline">{{ book.title }}</a>

                </h3>
                <p class="text-gray-600 mb-2">{{ book.author }}</p>
                <p class="text-red-600 font-bold">{{ book.price }} лв.</p>
            </div>
        </div>
        {% empty %}
        <p>Няма намерени книги в тази категория.</p>
        {% endfor %}
    </div>
</main>
{% endblock %}
